# Local Claude Code / MCP / Playwright workspace files
.claude/
.mcp.json
.playwright-mcp/

# Git worktrees (isolated per-task working copies)
.worktrees/

# Local scratch — playwright screenshots, verification shots, process boards
.scratch/
